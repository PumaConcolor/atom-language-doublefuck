'use babel';

import LanguageDoublefuckView from '../lib/language-doublefuck-view';

describe('LanguageDoublefuckView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
